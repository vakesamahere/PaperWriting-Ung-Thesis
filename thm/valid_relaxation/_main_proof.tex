\begin{proof}
我们需要证明完整模型 $\model{\discretizationFull}$ 的任意可行解能够由定理所描述的映射对应到松弛模型 $\model{\discretization, \itinSetSuper}$ 的可行解，且走松弛模型中得到的目标值不劣于原解。

证明分三个主要步骤进行。首先，准备必要的定义和基本分析工具。我们需要引入一个路径存在性引理，将宏观流量分解为单独的循环路径；还需要分析聚合映射 $\Phi$ 的核心性质，该映射全局地支配着原始网络与部分时空网络之间的结构关系。此外，还定义了证明所需的辅助集合和关系，以专门处理资源可用性约束的相关证明。其次，我们通过定理陈述中提到的映射 \eqref{eq:map_ori2rel}，从完整模型 $\model{\discretizationFull}$ 的任意可行解显式构造松弛模型 $\model{\discretization, \itinSetSuper}$ 的解。最后通过逐一检验每个约束和目标函数，验证所构造的解对 $\model{\discretization, \itinSetSuper}$ 可行且目标值不劣。

\input{thm/valid_relaxation/prepare.tex}

预备工具就绪后，我们现在正式将完整模型的任意可行解映射到松弛模型。
设 $\bar{\solution}=(\bar{\fleetAssignVar}, \bar{\shareVar}, \bar{\outsideShareVar})$ 为 $\model{\discretizationFull}$ 的任意可行解。

\paragraph*{在松弛模型中构造解}
我们通过定理陈述中提到的映射 \eqref{eq:map_ori2rel}，从 $\bar{\solution}$ 构造 $\model{\discretization, \itinSetSuper}$ 的解 $\solution = (\fleetAssignVar, \shareVar, \outsideShareVar)$。
由构造可知 $\fleetAssignVar_{af}\in\mathbb{Z}_{\ge0}$，
因此定义域条件~\eqref{constr:relax.domain.x.flight} 得到满足。
所有其他变量定义域要求保持不变。

剩下需要验证的是，所构造的解满足 $\model{\discretization, \itinSetSuper}$ 的所有运营约束和选择约束，并保持目标函数的有效下界。我们在下面逐一验证这些要求。
\input{thm/valid_relaxation/assign_flow.tex}
\input{thm/valid_relaxation/assign_fleet_available.tex}
\input{thm/valid_relaxation/assign_cooldown.tex}
\input{thm/valid_relaxation/assign_seg_freq.tex}
\input{thm/valid_relaxation/choice_ratio.tex}
\input{thm/valid_relaxation/choice_limit.tex}
\input{thm/valid_relaxation/choice_capacity.tex}
\input{thm/valid_relaxation/choice_covering.tex}
\input{thm/valid_relaxation/revenue_preserve.tex}

因此，对于完整模型的任意可行解，在聚合模型中存在目标值不劣的可行解，$\model{\discretization, \itinSetSuper}$ 提供了有效的下界。
\end{proof}
