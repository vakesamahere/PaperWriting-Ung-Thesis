\begin{theorem}
\label{thm:refine_converge}
The refinement strategy described in Subsection~\ref{sec:refine_process} will expand the time discretization $\discretization$ with at least 1 new time point if the relaxation solution is illegal in the original model. As a result, after finite iterations, the algorithm will converge to a globally optimal solution.
\end{theorem}